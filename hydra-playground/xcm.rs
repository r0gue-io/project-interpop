use ink::xcm::{
    prelude::{Asset, Junction::Parachain, Location, OriginKind, Weight, Xcm, XcmHash},
    VersionedXcm,
};

// fn form_transact_on_sibling_para(dest: u32, encoded_call: Vec<u8>) -> VersionedXcm<()> {
//
// }